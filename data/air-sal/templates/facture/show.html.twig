{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ facture._salle.address }}</td>
            </tr>
            <tr>
                <th>Data</th>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('facture_index') }}">back to list</a>
    <a href="{{ asset(file) }}" alt ="Download invoice"  download>download Invoice</a>
    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}